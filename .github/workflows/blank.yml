---
name: testing this file to call in another workflow

on:
  push: 
    branches: ["main"]
  pull-request:
    branches: ["main"]

#  workflow_dispatch:
  
jobs:
  test:
    runs-on: ubuntu-latest


    steps:
      - name: Test
        run: echo "Hurray"


